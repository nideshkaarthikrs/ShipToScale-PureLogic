# Copy to .env and fill in values. Do NOT commit the populated file.
# Primary LLM provider (per spec §9 — Claude API is the primary target)
# ANTHROPIC_API_KEY=

# Optional fallback providers
# OPENAI_API_KEY=
# GEMINI_API_KEY=
